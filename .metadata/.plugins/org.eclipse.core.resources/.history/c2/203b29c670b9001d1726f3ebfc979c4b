/**
 ******************************************************************************
 * @file           : main.c
 * @author         : Auto-generated by STM32CubeIDE
 * @brief          : Main program body
 ******************************************************************************
 * @attention
 *
 * Copyright (c) 2023 STMicroelectronics.
 * All rights reserved.
 *
 * This software is licensed under terms that can be found in the LICENSE file
 * in the root directory of this software component.
 * If no LICENSE file comes with this software, it is provided AS-IS.
 *
 ******************************************************************************
 */

#include <stdint.h>
#include <stdio.h>

#if !defined(__SOFT_FP__) && defined(__ARM_FP)
  #warning "FPU is not initialized, but the project is compiling for an FPU. Please initialize the FPU before use."
#endif

int main(void)
{
	asm volatile("SVC #8");
    /* Loop forever */
	for(;;);
}

__attribute__((naked)) void SVC_Handler(void){
	// Load the PC address into a register and pass to the next function for operation
	asm volatile("MRS R0, MSP");
	asm volatile("B svc_handler_2");
}

void svc_handler_2(uint32_t* sp_addr){
	uint32_t* pc_addr = (uint32_t*) sp_addr[6];
	pc_addr -= (uint32_t) 0x2;
	uint8_t svc_num = *pc_addr;
	printf("SVC number: %d\n", svc_num);

	// Increment the SVC number by 4
	svc_num += 4;
//	printf("At R0, the value in memory is: %lx\n", (sp_addr[0]));
//	printf("At R1, the value in memory is: %lx\n", (sp_addr[1]));
//	printf("At R2, the value in memory is: %lx\n", (sp_addr[2]));
//	printf("At R3, the value in memory is: %lx\n", (sp_addr[3]));
//	printf("At R12, the value in memory is: %lx\n", (sp_addr[4]));
//	printf("At LR, the value in memory is: %lx\n", (sp_addr[5]));
//	printf("At PC, the value in memory is: %lx\n", (sp_addr[6]));
//	printf("At XPSR, the value in memory is: %lx\n", (sp_addr[7]));

	// return to thread mode
//	asm volatile("B %0" : : "r"());
}
